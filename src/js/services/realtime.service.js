// Firestore snapshot listeners now handle real-time updates.
// This file stays only so older imports do not break if you extend the app later.
export function subscribe() {
  return () => {};
}

export function emitLocal() {}

export function closeRealtime() {}
